---
description: Đổi instance_id (và tên thư mục) của một desk tại chỗ, giữ mọi tham chiếu cũ resolve được: id cũ thành alias trong previous_ids. Không rewrite desk khác.
argument-hint: <desk> --to <new-id> [--name "..."] [--as "..."] [--why "..."]
allowed-tools: Read, Grep, Glob, Bash, Edit, Write, TodoWrite
---

# /aiws-agent-rename — pointer stub (generated at build — do not edit; CR-AIWS-2026-08-019)

Read and follow the authoritative verb spec: `.ai-work/agents/commands/aiws-agent-rename.md`.
Every gate in that spec (HUMAN confirm, aip_driven, no-auto-promotion, …) applies unchanged.
NL alternative: the `/aiws-agent` router skill.
