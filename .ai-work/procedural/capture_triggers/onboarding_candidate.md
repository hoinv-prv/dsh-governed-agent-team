# onboarding_candidate — Onboarding knowledge
- **type:** guideline_improvement_candidate · **suggested_target:** guideline · **timing:** normal
## When
AI nhận ra thông tin giúp một AI/HUMAN user mới bắt đầu nhanh hơn: overview nào nên đọc trước, setup caveats, smoke check, common flow, hoặc entry point.
## Capture as
**Family (CR-AIWS-2026-08-126 C1):** `wiki_knowledge_gap` — tên trigger `onboarding_candidate` giữ làm nhãn **capture-time**; giá trị ghi vào `candidate_kind` là `wiki_knowledge_gap`. `improvement_scope` mặc định: `project`.

candidate_kind: `wiki_knowledge_gap` — trigger này thuộc family `wiki_knowledge_gap` (CR-AIWS-2026-08-126 C1; tên trigger `onboarding_candidate` giữ làm nhãn capture-time, không còn là giá trị ghi vào `candidate_kind`). Capture tri thức onboarding: nên đọc gì trước, caveat khi setup, smoke check, luồng phổ biến, entry point.
## Suggested action
Sau HUMAN review: cập nhật onboarding guide hoặc overview để phản ánh tri thức bắt đầu nhanh này.
## Example
"Trước khi chạy task, new user nên đọc overview X làm entry point, rồi chạy smoke check Y để xác nhận môi trường."
## Notes
—
