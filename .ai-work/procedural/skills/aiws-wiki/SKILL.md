---
name: aiws-wiki
description: >
  Project Knowledge Hub domain — dựng, tra cứu và bảo trì wiki: Wiki Source Index, source meta,
  relations, mapping pattern (PMP), local knowledge, overview + trang curated. 14 verbs: lookup ·
  register · register-batch · build-meta · refresh · refresh-meta · deregister · build-pattern ·
  refresh-pattern · test-lookup · add-local-knowledge · build-overview · build-pages · bootstrap.
  Dùng khi: "tìm trong wiki", "tra cứu spec", "đăng ký tài liệu vào wiki", "cập nhật/refresh wiki",
  "build trang wiki" · "look up a spec", "register this file", "refresh wiki meta".
user-invocable: true
---

# SKILL: aiws-wiki (domain router)

> Consolidated by CR-AIWS-2026-07-025 — the former per-action skills of this domain are now VERBS of this ONE skill.
> **Old-name translation:** `aiws-wiki-<X>` → verb `X` (authority: `product/rename_map.json`).

## Router protocol (MANDATORY)
1. Parse the request → pick ONE verb from the table below (NL triggers).
2. **Read `operations/<verb>.md` (this folder) BEFORE executing** — it is the full, authoritative operation definition; every gate in it stays binding.
3. Verb mơ hồ → HỎI HUMAN (safety rule #6). Router adds NO gates, removes NONE.

## Verb routing table
| Verb | Operation | NL triggers | Params | Key gates (full text in operation file) |
|---|---|---|---|---|
| lookup | operations/lookup.md | tìm trong wiki / wiki có gì về X / tìm doc về X / tra cứu spec / lookup source / find doc in Knowledge Hub before raw read | --query --mode {lexical/id/path/tokens/catalog} --limit --full --excludes --scope --authorized --include-raw --lookup-mode object --source-type --system/--all-s | multi-system --system STOP-and-ask; raw-search authorization (CR-052) rc≠0 STOP; local scope rule-#11 gate; no-match escalation MANDATORY / never silent not-found; index-first no repo grep; catalog before raw; trip-wire capture BẮT BUỘC; confidence gate; no wh |
| register | operations/register.md | add file này vào wiki / đăng ký tài liệu này / thêm vào wiki index / register this file (+ folder & PMP phrasings route onward to register-batch / build-pattern) | file path + hint; derives source_id SRC-<PREFIX>-<ID>[-<LAYER>], artifact_type, profile, title; delegates aiws-doc-convert-skill for binaries | multi-system HUMAN prompt for system; classifier<0.75 HUMAN disambiguation MANDATORY; STAGE 2.4 param confirm MANDATORY; binary→partial; no _unknown type; object suggest-only unconditional / NEVER auto-build (DP6/INV-8); closing check + present tables |
| register-batch | operations/register-batch.md | add toàn bộ file trong thư mục vào wiki / register wiki sources batch / bulk ingestion / folder of design files | <folder> / --files ...; --incremental; route_build_tool.py get/render/set; smoke_test --self-test --only | GATE 1 plan review ALWAYS mandatory; GATE 2 sample review on new format; BLOCKING GATE 4 PMP+route decisions (batch not complete until logged); Rule 8 no self-initiated builder/route (W2 needs explicit HUMAN directive); custom builder never writes index.jsonl; |
| build-meta | operations/build-meta.md | thêm source mới vào wiki / tạo meta cho source / đăng ký tài liệu mới vào wiki index / build wiki source meta / register new document | --artifact --source-id --source-type --profile --title [--summary --knowledge-targets --lookup-keys] --system/--common --mode {create/refresh} --no-related-sour | multi-system --system/--common tool ERROR + HUMAN prompt; format-mismatch HUMAN confirm (never silent low-quality meta); object metas hand-author-only suggest-only (DP6/INV-8/rule #7; ngoại lệ hẹp cho PO-approved deterministic build route — KOM Spec v0.4 §3bis.5(7)); unconditional object/relation capture + Step-5 present-for-confirm; Write-t |
| refresh | operations/refresh.md | update/refresh/cập nhật wiki / file đã thay đổi / source đã thay đổi / sync wiki source / tài liệu gốc đã update (PMP phrasings route onward to refresh-pattern) | delegates detect/refresh/evaluate tools; wiki_relations --relations before edge changes; build_relations.py; build_wiki_overview.py --system/--all-systems | Promotion Gate MANDATORY classification + HARD STOP + promotion log + no apply without CR+wiki-manager; relations impact check FIRST (IN-edges); tooling never rewrites wiki entries; no candidate promotion without HUMAN review; overview regen hook + overview_ha |
| refresh-meta | operations/refresh-meta.md | cập nhật wiki meta / refresh meta / source đã thay đổi / periodic wiki maintenance | --meta --profile [--apply] [--regenerate-summary] [--regenerate-lookup-keys] [--review-decision] [--impact-level] [--change-summary] | CR-S6 Promotion Gate HARD STOP (5 triggers, _promotion_log.jsonl, DO NOT apply); material change ≠ approval; candidate/review before wiki updates; preserve resolved Related Sources; union-merge curated keys (never drop); object re-check suggest-only + present  |
| deregister | operations/deregister.md | gỡ source khỏi wiki / xóa tài liệu khỏi wiki index / bỏ đăng ký source / dọn orphan registration / deregister wiki source / remove this source / clean up dangling meta | source_id / list / folder; archive(default)/delete/cancel; append_maintenance_log action=source_deregistered; index+relations rebuild | STAGE 2 HUMAN gate MANDATORY (explicit confirm, cancel=stop); audit MANDATORY via append_maintenance_log (never hand-JSON); resolve via index never guess paths; archive default; object still-referenced → EXTRA explicit confirm; no dangling index entry |
| build-pattern | operations/build-pattern.md | tạo mapping pattern / save format pattern / build PMP / (AI-triggered: new stable format detected) | N sample metas → pmp_<profile_id>.yml in profiles/; mapping_pattern_id PMP-<PROJECT>-<FORMAT>-<VERSION> | human-confirmed before save (high/medium confidence needs explicit confirm); generic format_signature; single-sample only at low confidence |
| refresh-pattern | operations/refresh-pattern.md | cập nhật mapping pattern / refresh PMP / format đã thay đổi / format drift detected | existing pmp_<profile_id>.yml + drift samples; bump last_validated | show diff → user confirms before save; breaking drift → NEW PMP never patch; retired PMP deprecated+superseded_by never delete; low confidence → sample-first next build |
| test-lookup | operations/test-lookup.md | test/kiểm tra wiki lookup / thử lookup / verify lookup / lookup có tìm ra không / check if wiki can find / test xem wiki tìm được gì cho tác vụ | Mode A: --self-test [--verbose --top-n --key-limit]; Mode B: --cases <jsonl> [--format json] (fields query/expected_source_id/mode/top_n/label/system/forbid); M | report-and-suggest only, never self-edit meta/index; Mode C MUST run the real tool (no inferred results); user decides improvements; rebuild index before re-test |
| add-local-knowledge | operations/add-local-knowledge.md | thêm knowledge source / đăng ký nguồn kiến thức local / add external source / register local knowledge / có tài liệu bên ngoài muốn AI biết | meta_dir label description search_triggers; build_wiki_source_index.py --scope local | never remove index.local.sources.json entries; always rebuild local index; always update LOCAL_KNOWLEDGE_OVERVIEW.md (primary relevance filter); no content embedding; downstream search stays rule-#10/#11 gated |
| build-overview | operations/build-overview.md | tạo wiki overview / regenerate overview / cập nhật trang tổng quan wiki / wiki có gì / after index-relations rebuild / lint overview_fingerprint_stale | build_wiki_overview.py --system <id>/--all-systems [--pages contents,search,health] [--no-register] | multi-system --system/--all-systems REQUIRED; page catalog CR-ratified (add/remove default-on page = new CR); narrative pages NOT this tool (HUMAN gate each); never hand-edit generated pages (overview_hand_edit ERROR); new page proposals capture-only |
| build-pages | operations/build-pages.md | build trang wiki dự án / tạo trang tổng hợp wiki / system overview / function list / business flow page / onboarding pages | build_wiki_page_base.py {survey/skeleton/check-grounding} --system/--all-systems --page <KIND> --out <ws>; subverbs survey/build/register/refresh/status | Gate 1 PAGE PLAN STOP for HUMAN subset; Gate 2 draft STOP; register only HUMAN-approved (rejected stays draft); two-clause no auto-regen; candidate-only #7 + Controlled Promotion STOP+CR; per-system hard-require; directory map registered-dirs-only no raw scan; |
| bootstrap | operations/bootstrap.md | bootstrap wiki / build project Knowledge Hub lần đầu / khởi tạo wiki dự án / first wiki build (ONE-TIME, not maintenance) | $1 optional corpus-root; lint_aip.py 0-error gate; build_wiki_overview.py at STEP-11 | Pre-flight HARD multi-system STOP-and-ask; first-build-only confirm if wiki exists; FIXED 12-step skeleton none added/removed, never author AIP free-form; Step B Q&A gate never guess; scope discipline no path pinning by exploration; never synthesize identity n |
