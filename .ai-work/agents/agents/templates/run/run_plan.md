# Run Plan — <RUN-id>  (two-phase run, CR-AIWS-2026-07-016)

> Working-AIP-Lite shape (Working_AIP_Connection_Spec_MVP §5). Drafted by the AGENT in phase 1;
> HUMAN confirms via `confirm-plan` (run_state records by/at) — then execution runs with NO
> mid-run gate (OPs = log-and-continue; only a blocking OP stops).
> Copied into the run-folder ONLY when plan_first is on.

## Task (VERBATIM — do not paraphrase; this is the durable task record)
<task verbatim>

## Expected Output
- <deliverable(s) + where in output/>

## Context / Source References (resolved BEFORE new lookups: check QA task_info_pack + reading kits)
| Ref | SoT layer | Role in task | Load mode |
|---|---|---|---|
| (fill) | | | pointer/full |

## Steps
1. <step — run_state.progress derives from THIS list after confirm>
2. ...

## Guardrails
- <task-specific + blueprint non_responsibilities>

## Proposed confirm points
- (default: NONE mid-run — plan-level confirm only; list exceptions needing HUMAN mid-run)

## Open questions (log-and-continue unless blocking)
- <OP or "(none)">

## Autonomy note
- OPs discovered mid-run are LOGGED and worked around; only a blocking OP stops the run.

## Done criteria
- <checkable done conditions>
